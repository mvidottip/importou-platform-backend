export enum RoleStatus {
  Created = "created",
  Activated = "activated",
  Deactivated = "deactivated",
}

export enum RoleType {
  Admin = "admin",
  Importer = "importer",
  Exporter = "exporter",
  Broker = "broker",
}
