NODE_ENV=development
PORT=3000
# Local Postgres (pgAdmin): porta 5432. Docker compose (opcional): porta 5433.
# Nest/dotenv NÃO expande ${VAR} — coloque a URL completa.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/importou?schema=public
JWT_SECRET=change-me-importou-dev
